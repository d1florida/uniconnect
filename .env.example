# PostgreSQL (docker-compose)
POSTGRES_USER=uniconnect
POSTGRES_PASSWORD=uniconnect
POSTGRES_DB=uniconnect

# API connection string (matches docker-compose defaults)
ConnectionStrings__Default=Host=localhost;Port=5433;Database=uniconnect;Username=uniconnect;Password=uniconnect

# React dev
VITE_API_BASE_URL=http://localhost:5000
